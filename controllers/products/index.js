const { getProduct, getProductbyId, getMyProduct, getMyProductbyId, handleCreateProduct } = require('./product');

module.exports = {
  getProduct,
  getProductbyId,
  getMyProduct,
  getMyProductbyId,
  handleCreateProduct,
};
